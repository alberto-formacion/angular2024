import { BuscarContactoPipe } from './buscar-contacto.pipe';

describe('BuscarContactoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarContactoPipe();
    expect(pipe).toBeTruthy();
  });
});
