import { OrdenarContactoPipe } from './ordenar-contacto.pipe';

describe('OrdenarContactoPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarContactoPipe();
    expect(pipe).toBeTruthy();
  });
});
